# 线上配置文件
server:
  port: 8101
spring:
  # 数据库配置
  # todo 需替换配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/aigc
    username: root
    password: 158574
  # Redis 配置
  # todo 需替换配置
  redis:
    database: 1
    host: 192.168.161.128
    port: 6379
    timeout: 5000
  # Elasticsearch 配置
  # todo 需替换配置
  elasticsearch:
    uris: http://192.168.161.128:9200
    username: elastic
    password: 158574
mybatis-plus:
  configuration:
    # 生产环境关闭日志
    log-impl: ''